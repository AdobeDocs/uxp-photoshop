---
title: ":nth-last-of-type"
description: Matches only the Nth child of a given type from the end. Can use an expression or even or odd.
jsDoc: true
---
# :nth-last-of-type

**Since** UXP v3.0

Matches only the Nth child of a given type from the end. Can use an expression or `even` or `odd`.

**See:** [https://developer.mozilla.org/en-US/docs/Web/CSS/:nth-last-of-type](https://developer.mozilla.org/en-US/docs/Web/CSS/:nth-last-of-type)

**Example:**

```css
.row:nth-last-of-type(even) {
    background-color: #E8E8E8; 
}
```

